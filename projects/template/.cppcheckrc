# Cppcheck configuration file

# Enable all checks
--enable=all

# Suppress common false positives
--suppress=missingIncludeSystem
--suppress=unusedFunction
--suppress=unmatchedSuppression

# Use inline suppressions
--inline-suppr

# Check unused functions (can be disabled if too noisy)
--check-unused-suppressions

# Platform-specific settings
--platform=unix64

# Output format
--xml
--xml-version=2

# Include paths (relative to project root)
-I include

# Exclude patterns
--suppress=*:build/*
--suppress=*:tests/*

